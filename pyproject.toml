[project]
name = "incident-webhook"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "python-dotenv",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["webhook*"]

[tool.pytest.ini_options]
pythonpath = ["."]
